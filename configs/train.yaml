# configs/train.yaml
# Training configuration for XGBoost energy demand forecasting model

features:
  - load_ma_3h
  - temperature_ma_3h

target: load

test_size: 0.2
random_state: 42

model_params:
  n_estimators: 100
  learning_rate: 0.1
  max_depth: 5
